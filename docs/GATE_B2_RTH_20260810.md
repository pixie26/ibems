# Gate B2 SPY RTH 行情与 Recorder 证据（2026-08-10）

## 1. 结论

**RTH 行情与 bounded Recorder 子项 PASS；Gate B2 整体仍未 PASS。**

2026-08-10 香港时间约 23:01–23:07，在真实 IB Gateway paper account、Gateway Read-Only API 开启、Python `readonly=True`、零订单条件下，使用 `SPY / SMART / ARCA / USD` 完成：

1. 120 秒 event-driven BidAsk / AllLast / 5 秒 bars preflight；
2. 120 秒真实 `QuoteRecorder` subscription、event handler 与 append-only gzip `RawEventLog` 写盘。

两轮均观察到 `marketDataType=1`（LIVE），三路数据均非零。本结论只证明本次 paper Gateway 环境中的 SPY RTH 只读行情路径；它不是 Full-RTH 全日覆盖报告，不证明订单协议、非空 reconciliation 或 late/duplicate/out-of-order order callback。

## 2. v1：competing session 与判定缺口

RTH preflight v1 使用 `clientId=947`。三路已经收到数据，但采样约 68.078 秒时 Gateway 返回 `10197`：存在竞争的真实账户交易会话，无法继续取得市场数据。该轮按 fail-closed 判为无效，不能用前 68 秒的非零计数签署 RTH PASS。

v1 同时暴露一个测试工具缺口：报告正确记录了 `entitlement_blocked=true`，采样也提前结束，但旧的 overall checks 没有把这两个事实列为否决条件，因而错误写出 `passed=true`。随后增加两个机械检查：

- `no_fatal_entitlement_error`：任何 fatal market-data / entitlement error 都否决；
- `sample_window_complete`：实际窗口必须达到请求时长。

回归测试直接覆盖“已经有大量 LIVE ticks、随后出现 10197、窗口不完整”的组合；preflight 专项测试更新为 `20 passed`。v1 失败证据没有被覆盖：

- report：`artifacts/ib_preflight/20260810_b2_rth_market_v1/report.json`
- report SHA-256：`db640ef3fec57adadac81426b314afcc0cc28ef31cb70f9243ab9d3989d34143`
- 实际执行脚本 SHA-256：`824004cc59b62ae5ad58a54eb54bd648b7c67def5fda3002871dbca8b29c54c9`

## 3. RTH preflight v2

operator 调整竞争会话设置后，使用新 `clientId=948` 和修正后的判定重新执行：

| 字段 | 直接观测 |
|---|---:|
| route | `session_label=RTH`；`exchange=SMART` |
| contract | `conId=756733`；`SPY / SMART / ARCA / USD` |
| market data type | `1`（LIVE） |
| sample window | 120.109 秒 |
| BidAsk | 25,665 |
| AllLast | 3,168 |
| 5 秒 bars | 25 |
| fatal entitlement error | 0 |
| broker snapshot | 三轮 `positions/open orders/executions = 0/0/0`，相邻 hash 一致 |

六项检查全部为 true：稳定 snapshot、LIVE、三路非零、clock skew 阈值、无 fatal entitlement error、完整采样窗口；`passed=true`。

- report：`artifacts/ib_preflight/20260810_b2_rth_market_v2/report.json`
- report SHA-256：`204c979b8c29670a33dfdba111e704f51d6971dddc6989b4214ac0e8ad914c1b`
- 实际执行脚本 SHA-256：`a76bcc71b69756e242de26d55a1198c872a842005f8f739157ac2037d75e1cdb`

## 4. bounded Recorder RTH 写盘

使用 `clientId=949` 复用真实：

- `QuoteRecorder._subscribe`
- `QuoteRecorder._wire_ticker`
- `QuoteRecorder._wire_bars`
- append-only gzip `RawEventLog`

该 bounded probe 不调用全日 `finalize_day`，所以不把两分钟样本伪装成 Full-RTH health。执行 120.360 秒，无异常、无 fatal market-data error，`broker_write_calls=[]`。

关闭 gzip 后独立重读得到：

| Raw event type | 行数 | market data type |
|---|---:|---|
| BidAsk | 15,590 | LIVE |
| AllLast | 2,843 | LIVE |
| 5 秒 bars | 25 | LIVE |
| SYSTEM | 3 | 首条连接记录为 UNKNOWN，market-data callback 后两条为 LIVE |
| 合计 | 18,461 | — |

因此 raw log 中出现的 `UNKNOWN` 只属于获得 market-data-type callback 之前的首条 SYSTEM 连接记录；所有三类市场数据行都是 LIVE。

- report：`artifacts/ib_preflight/20260810_b2_rth_recorder_v1/report.json`
- report SHA-256：`86de43714c1d9fa1f515d01ddc126589b11e1634ff23f1ef57ca439cfa9a543d`
- raw segment SHA-256：`88acd19610d4099e7fddb4122a2857e34d9361b4812b5ea4fb83ab8db160e613`
- raw close segment SHA-256：`eeb223ffea7d5b2ff971b49c01b362296b1e86bafd7ffa51ddbabed7422f9175`
- 实际 Recorder source SHA-256：`bf7d7b54a8d4b1b23a3f1fdd36cf0c53b9bde760cf9bc41d9cbfd6655c4d5321`
- probe core SHA-256：`57726607891f9b78dc23312af56c5746844ab9e22f57076570e3b0a7e1446df0`
- RTH wrapper SHA-256：`ced6e426091a1c24af1d334c50f63cfa7e8ee1e24433e90e9fe6838587b4d2ce`

三个 evidence 目录均包含 `SHA256SUMS`，固定报告、实际执行代码副本和 Recorder raw segments。

## 5. 仍未证明的事项

- 两分钟 bounded run 不证明完整 RTH session coverage、整日 gap thresholds 或收盘 finalize。
- v2 的 broker snapshot 仍为空状态，不证明非空动态 reconciliation。
- 没有调用任何订单 API；没有验证 `orderId / permId / clientId / orderRef`、submit/ack/modify/cancel/fill 或 commission。
- 没有在本轮活跃行情中再次执行 Gateway restart 或外网中断；既有 `1100 -> 1102` 证据仍是独立轮次，`1101` 仍未直接观察。
- `completed orders` 继续被 Gateway Read-Only policy 阻断；不得关闭 Read-Only 追测。
- Gate B2 尚需完成 technical review、官方 documented-vs-observed 复核、Windows/full-suite gap 处置和新的可复查 freeze。
